// Type definitions for the project
import '@testing-library/jest-dom';

// Declare Jest as a global
declare global {
  const jest: any;
  const expect: any;
  const test: any;
  const describe: any;
  const it: any;
  const beforeAll: any;
  const afterAll: any;
  const beforeEach: any;
  const afterEach: any;
} 